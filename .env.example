# =============================================================================
# PYX Scanner — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# --- Supabase -----------------------------------------------------------------
# Your Supabase project URL and service role key.
# Get these from: https://supabase.com/dashboard/project/_/settings/api
# WARNING: The service role key bypasses RLS. Never expose it client-side.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_your-service-role-key

# Public anon key — safe to expose, respects RLS policies.
# Used by OG image generation and widget routes for read-only access.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- PYX Admin (optional) -----------------------------------------------------
# Only needed by PYX team for running scans. Contributors do NOT need these.
# PYX_API_URL is the base URL for the scan API.
# PYX_ADMIN_API_KEY authenticates admin-only endpoints (POST /api/v1/scan, POST /api/v1/scan-result).

PYX_API_URL=http://localhost:3000
# PYX_ADMIN_API_KEY=your-admin-api-key

# --- GitHub -------------------------------------------------------------------
# Optional but recommended. Increases GitHub API rate limits from 60 to 5000/hr.
# Create a token at: https://github.com/settings/tokens

GITHUB_TOKEN=ghp_your-token

# --- Anthropic ----------------------------------------------------------------
# Required for running scans. Get your key at: https://console.anthropic.com/

# ANTHROPIC_API_KEY=sk-ant-your-key

# --- Database (optional) ------------------------------------------------------
# Only needed if running migrations directly against the database.

# DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:6543/postgres
# DIRECT_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres
